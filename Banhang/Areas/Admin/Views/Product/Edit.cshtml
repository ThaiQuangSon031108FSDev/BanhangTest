@model Banhang.Models.Product
@{
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
    ViewData["Title"] = "Chỉnh sửa sản phẩm";
    var categories = ViewBag.Categories as List<Banhang.Models.Category>;
}

<partial name="_Breadcrumb" />

<h4>Chỉnh sửa sản phẩm</h4>

@if (ViewBag.Error != null)
{
    <div class="alert alert-danger">@ViewBag.Error</div>
}

<form method="post" enctype="multipart/form-data">
    @Html.AntiForgeryToken()
    <input type="hidden" asp-for="ProductID" />
    <div class="row">
        <div class="col-md-6 mb-3">
            <label class="form-label">Tên sản phẩm</label>
            <input class="form-control" asp-for="ProductName" required />
        </div>
        <div class="col-md-3 mb-3">
            <label class="form-label">Giá</label>
            <input class="form-control" asp-for="Price" type="number" step="0.01" required />
        </div>
        <div class="col-md-3 mb-3">
            <label class="form-label">Tồn kho</label>
            <input class="form-control" asp-for="Stock" type="number" />
        </div>
        <div class="col-md-6 mb-3">
            <label class="form-label">Danh mục</label>
            <select class="form-select" asp-for="CategoryID">
                @foreach (var c in categories)
                {
                    <option value="@c.CategoryID" @(c.CategoryID == Model.CategoryID ? "selected" : null)>@c.CategoryName</option>
                }
            </select>
        </div>

        <div class="col-md-6 mb-3">
            <label class="form-label">Upload ảnh</label>
            <input class="form-control" type="file" name="imageFile" accept="image/*" id="imageFile" />
            <small class="text-muted">Chấp nhận: JPG, PNG, GIF. Tối đa 5MB</small>
        </div>

        <div class="col-12 mb-3">
            <img id="imagePreview" src="@(string.IsNullOrEmpty(Model.ImageUrl) ? "#" : Model.ImageUrl)" alt="Preview" class="img-thumbnail @(string.IsNullOrEmpty(Model.ImageUrl) ? "d-none" : "")" style="max-height: 200px;" />
        </div>

        <div class="col-md-12 mb-3">
            <label class="form-label">Hoặc nhập URL ảnh</label>
            <input class="form-control" asp-for="ImageUrl" placeholder="https://example.com/image.jpg" />
        </div>

        <div class="col-md-4 mb-3">
            <label class="form-label">Màu</label>
            <input class="form-control" asp-for="Color" />
        </div>
        <div class="col-md-4 mb-3">
            <label class="form-label">Size</label>
            <input class="form-control" asp-for="Size" />
        </div>
        <div class="col-md-4 mb-3 d-flex align-items-end">
            <div class="form-check">
                <input class="form-check-input" type="checkbox" asp-for="IsActive" />
                <label class="form-check-label" asp-for="IsActive">Kích hoạt</label>
            </div>
        </div>
        <div class="col-12 mb-3">
            <label class="form-label">Mô tả</label>
            <textarea class="form-control" asp-for="Description" rows="4"></textarea>
        </div>
    </div>
    <button class="btn btn-success">💾 Lưu</button>
    <a class="btn btn-secondary" asp-action="Index">❌ Hủy</a>
</form>

@section Scripts {
    <script>
        document.getElementById('imageFile').addEventListener('change', function(e) {
            const file = e.target.files[0];
            if (file) {
                const reader = new FileReader();
                reader.onload = function(e) {
                    const preview = document.getElementById('imagePreview');
                    preview.src = e.target.result;
                    preview.classList.remove('d-none');
                };
                reader.readAsDataURL(file);
            }
        });
    </script>
}
