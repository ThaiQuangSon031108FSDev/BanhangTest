@model List<Banhang.Models.Product>
@{
    ViewData["Title"] = "Trang chủ";
    var categories = ViewBag.Categories as List<Banhang.Models.Category>;
}

<div class="animated-background">
    <div class="floating-shape shape1"></div>
    <div class="floating-shape shape2"></div>
    <div class="floating-shape shape3"></div>
</div>

<section class="hero-section">
    <div class="container">
        <div class="row align-items-center">
            <div class="col-lg-6 hero-content">
                <h1 class="hero-title">Mua Sắm<br>Thông Minh</h1>
                <p class="hero-subtitle">Khám phá hàng ngàn sản phẩm chất lượng với giá tốt nhất thị trường</p>
                <a href="#products" class="hero-btn">
                    <span style="position: relative; z-index: 1;">Khám phá ngay 🚀</span>
                </a>
            </div>
        </div>
    </div>
    <div class="hero-3d">
        <div class="cube-3d">
            <div class="cube-face" style="transform: rotateY(0deg) translateZ(100px);"></div>
            <div class="cube-face" style="transform: rotateY(90deg) translateZ(100px);"></div>
            <div class="cube-face" style="transform: rotateY(180deg) translateZ(100px);"></div>
            <div class="cube-face" style="transform: rotateY(-90deg) translateZ(100px);"></div>
            <div class="cube-face" style="transform: rotateX(90deg) translateZ(100px);"></div>
            <div class="cube-face" style="transform: rotateX(-90deg) translateZ(100px);"></div>
        </div>
    </div>
</section>

<section class="category-section" id="categories">
    <div class="container">
        <h2 class="section-title fade-in-observer">📂 Danh Mục Sản Phẩm</h2>
        <div class="row g-4">
            @if (categories != null && categories.Any())
            {
                foreach (var cat in categories)
                {
                    <div class="col-6 col-md-4 col-lg-2 fade-in-observer">
                        <a href="?categoryId=@cat.CategoryID" class="text-decoration-none">
                            <div class="category-card">
                                <div class="category-icon">
                                    @switch(cat.CategoryName.ToLower())
                                    {
                                        case "điện thoại": <text>📱</text> break;
                                        case "laptop": <text>💻</text> break;
                                        case "tablet": <text>📲</text> break;
                                        case "phụ kiện": <text>🎧</text> break;
                                        case "đồng hồ": <text>⌚</text> break;
                                        default: <text>📦</text> break;
                                    }
                                </div>
                                <div class="category-name">@cat.CategoryName</div>
                            </div>
                        </a>
                    </div>
                }
            }
        </div>
    </div>
</section>

<section class="product-section" id="products">
    <div class="container">
        <h2 class="section-title fade-in-observer">⭐ Sản Phẩm Nổi Bật</h2>
        
        @if (Model == null || !Model.Any())
        {
            <div class="alert alert-info text-center py-5">
                <h4>😔 Chưa có sản phẩm nào</h4>
            </div>
        }
        else
        {
            <div class="row g-4">
                @foreach (var p in Model)
                {
                    <div class="col-6 col-md-4 col-lg-3 fade-in-observer">
                        <div class="product-card">
                            <div class="product-img-wrapper">
                                <img src="@(string.IsNullOrEmpty(p.ImageUrl) ? "https://via.placeholder.com/400" : p.ImageUrl)" 
                                     alt="@p.ProductName" 
                                     class="product-img">
                                @if (p.Stock < 10)
                                {
                                    <span class="product-badge">Sắp hết 🔥</span>
                                }
                                else
                                {
                                    <span class="product-badge">Hot 🔥</span>
                                }
                            </div>
                            <div class="product-body">
                                <h3 class="product-title">@p.ProductName</h3>
                                <div class="product-price">@p.Price.ToString("#,0")đ</div>
                                <a asp-action="Details" asp-route-id="@p.ProductID" class="product-btn" style="text-decoration: none; display: block;">
                                    Xem chi tiết
                                </a>
                            </div>
                        </div>
                    </div>
                }
            </div>
        }
    </div>
</section>

<div style="height: 100px;"></div>

@section Scripts {
    <script>
        // Scroll Progress Indicator
        const scrollIndicator = document.createElement('div');
        scrollIndicator.className = 'scroll-indicator';
        scrollIndicator.id = 'scrollIndicator';
        document.body.appendChild(scrollIndicator);

        // Loading Screen
        window.addEventListener('load', () => {
            setTimeout(() => {
                const loader = document.getElementById('loader');
                if (loader) {
                    loader.classList.add('hidden');
                }
            }, 1000);
        });

        window.addEventListener('scroll', () => {
            // Progress bar
            const winScroll = document.documentElement.scrollTop;
            const height = document.documentElement.scrollHeight - document.documentElement.clientHeight;
            const scrolled = (winScroll / height) * 100;
            const indicator = document.getElementById('scrollIndicator');
            if (indicator) {
                indicator.style.width = scrolled + '%';
            }
        });

        // Intersection Observer for fade-in
        const observerOptions = {
            threshold: 0.1,
            rootMargin: '0px 0px -100px 0px'
        };

        const observer = new IntersectionObserver((entries) => {
            entries.forEach((entry, index) => {
                if (entry.isIntersecting) {
                    setTimeout(() => {
                        entry.target.classList.add('visible');
                    }, index * 100);
                }
            });
        }, observerOptions);

        document.querySelectorAll('.fade-in-observer').forEach(el => {
            observer.observe(el);
        });

        // Parallax for shapes
        document.addEventListener('mousemove', (e) => {
            const shapes = document.querySelectorAll('.floating-shape');
            const x = e.clientX / window.innerWidth;
            const y = e.clientY / window.innerHeight;

            shapes.forEach((shape, index) => {
                const speed = (index + 1) * 20;
                const xOffset = (x - 0.5) * speed;
                const yOffset = (y - 0.5) * speed;
                shape.style.transform = `translate(${xOffset}px, ${yOffset}px)`;
            });
        });

        // Product card 3D tilt
        document.querySelectorAll('.product-card').forEach(card => {
            card.addEventListener('mousemove', (e) => {
                const rect = card.getBoundingClientRect();
                const x = e.clientX - rect.left;
                const y = e.clientY - rect.top;
                const centerX = rect.width / 2;
                const centerY = rect.height / 2;
                const rotateX = (y - centerY) / 10;
                const rotateY = (centerX - x) / 10;
                card.style.transform = `perspective(1000px) rotateX(${rotateX}deg) rotateY(${rotateY}deg) translateY(-20px)`;
            });
            card.addEventListener('mouseleave', () => {
                card.style.transform = 'perspective(1000px) rotateX(0) rotateY(0) translateY(0)';
            });
        });

        // Smooth scroll
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function (e) {
                e.preventDefault();
                const target = document.querySelector(this.getAttribute('href'));
                if (target) {
                    target.scrollIntoView({
                        behavior: 'smooth',
                        block: 'start'
                    });
                }
            });
        });
    </script>
}
