@{
    ViewData["Title"] = "Đăng nhập";
    var remembered = Context.Request.Cookies["remember_username"];
}

@await Html.PartialAsync("_PremiumHero", new PremiumHeroViewModel
{
    Eyebrow = "Trở lại mua sắm",
    Title = "Chào mừng trở lại! 🔐",
    Subtitle = "Đăng nhập để tiếp tục mua sắm với trải nghiệm Ultra Premium."
})

<section class="py-5">
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-lg-5">
                <div class="glass-card p-4">
                    @if (ViewBag.Error != null)
                    {
                        <div class="alert alert-danger">@ViewBag.Error</div>
                    }
                    <form asp-action="Login" method="post" data-loading="true">
                        @Html.AntiForgeryToken()
                        <input type="hidden" name="returnUrl" value="@(ViewBag.ReturnUrl ?? string.Empty)" />
                        <div class="mb-4">
                            <label class="form-label text-white">Tên đăng nhập</label>
                            <div class="input-group input-group-lg">
                                <span class="input-group-text">👤</span>
                                <input class="form-control" name="username" value="@remembered" placeholder="Nhập tên đăng nhập" required />
                            </div>
                        </div>
                        <div class="mb-4">
                            <label class="form-label text-white">Mật khẩu</label>
                            <div class="input-group input-group-lg">
                                <span class="input-group-text">🔑</span>
                                <input class="form-control" type="password" name="password" placeholder="Nhập mật khẩu" required />
                            </div>
                        </div>
                        <div class="form-check mb-4">
                            <input class="form-check-input" type="checkbox" name="remember" id="remember" />
                            <label class="form-check-label text-white-50" for="remember">Ghi nhớ đăng nhập</label>
                        </div>
                        <div class="d-grid gap-3">
                            <button class="btn btn-gradient btn-lg" type="submit">Đăng nhập</button>
                            <a class="btn btn-outline-primary btn-lg" asp-action="Register">Tạo tài khoản mới</a>
                            <a class="text-white-50 text-center" asp-action="ForgotPassword">Quên mật khẩu?</a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</section>
