@model Banhang.Models.ViewModels.ForgotPasswordViewModel
@{
    ViewData["Title"] = "Quên mật khẩu";
}

<div class="page-hero gradient-bg py-5 text-white text-center position-relative overflow-hidden">
    <div class="floating-shape shape1"></div>
    <div class="floating-shape shape2"></div>
    <div class="floating-shape shape3"></div>
    <div class="container position-relative">
        <h1 class="display-5 fw-bold mb-2">Quên mật khẩu?</h1>
        <p class="lead mb-0">Nhập email để nhận liên kết đặt lại mật khẩu.</p>
    </div>
</div>

<section class="py-5">
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-lg-5">
                <div class="glass-card p-4">
                    <form method="post" data-loading="true">
                        @Html.AntiForgeryToken()
                        <div class="mb-4">
                            <label class="form-label text-white" asp-for="Email"></label>
                            <div class="input-group input-group-lg">
                                <span class="input-group-text">📧</span>
                                <input class="form-control" asp-for="Email" placeholder="you@example.com" />
                            </div>
                            <span class="text-warning" asp-validation-for="Email"></span>
                        </div>
                        <div class="d-grid gap-3">
                            <button class="btn btn-gradient btn-lg" type="submit">Gửi liên kết đặt lại</button>
                            <a class="btn btn-outline-light btn-lg" asp-action="Login">Quay lại đăng nhập</a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</section>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
